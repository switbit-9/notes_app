import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cardStyle: {
    margin: 10,
    padding: 10,
  },
  fab: {
    position: "absolute",
    margin: 16,
    right: 0,
    bottom: 0,
  },
});

export default styles;
