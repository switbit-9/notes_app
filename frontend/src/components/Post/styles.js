import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  cardStyle: {
    margin: 10,
    padding: 10,
  },
  toggleButton: {
    color: "blue",
    marginTop: 10,
  },
});

export default styles;
